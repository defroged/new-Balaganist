From 5cc8a98927f1b90362c9bd532923eb440824b836 Mon Sep 17 00:00:00 2001
From: OpenAI Codex <codex@openai.com>
Date: Tue, 21 Jul 2026 22:12:39 +0800
Subject: [PATCH] Add album detail cards

---
 css/style.css       | 471 +++++++++++++++++++++++++------------------
 index.html          | 118 ++++++-----
 js/mainFunctions.js | 475 ++++++++++++++++++++++++++++++++++++--------
 3 files changed, 736 insertions(+), 328 deletions(-)

diff --git a/css/style.css b/css/style.css
index 673b5b9..48451b4 100644
--- a/css/style.css
+++ b/css/style.css
@@ -42,7 +42,7 @@ body {
 
 body.nav-open,
 body.bio-open,
-body.album-open {
+body.album-details-open {
   overflow: hidden;
 }
 
@@ -977,11 +977,42 @@ main,
 .album-insert__title {
   position: relative;
   z-index: 1;
+  display: flex;
+  width: 100%;
+  align-items: baseline;
+  justify-content: space-between;
+  gap: 12px;
+  padding: 4px 0;
   margin: 5px 0 0;
   color: #17130b;
+  border: 0;
+  background: transparent;
   font-family: var(--font-display);
   font-size: clamp(1rem, 2vw, 1.45rem);
   line-height: 1.2;
+  text-align: left;
+  cursor: pointer;
+  transition: color 180ms ease;
+}
+
+.album-insert__title-action {
+  color: #806517;
+  font-size: clamp(0.42rem, 0.65vw, 0.54rem);
+  font-weight: 600;
+  letter-spacing: 0.08em;
+  text-transform: uppercase;
+  white-space: nowrap;
+  transition: transform 180ms ease;
+}
+
+.album-insert__title:hover,
+.album-insert__title:focus-visible {
+  color: #806517;
+}
+
+.album-insert__title:hover .album-insert__title-action,
+.album-insert__title:focus-visible .album-insert__title-action {
+  transform: translate(2px, -1px);
 }
 
 .album-insert__catalog {
@@ -1173,7 +1204,8 @@ main,
 }
 
 .track-list--insert li {
-  grid-template-columns: 24px minmax(0, 1fr) auto;
+  grid-template-columns: 24px 28px minmax(0, 1fr);
+  gap: 6px;
   padding: clamp(6px, 1vw, 9px) 0;
   color: #292216;
   border-bottom-color: rgba(61, 47, 17, 0.18);
@@ -1186,6 +1218,85 @@ main,
   color: #806517;
 }
 
+.track-list__play,
+.track-list__details {
+  min-width: 0;
+  padding: 0;
+  color: inherit;
+  border: 0;
+  background: transparent;
+  cursor: pointer;
+}
+
+.track-list__play {
+  position: relative;
+  display: grid;
+  width: 26px;
+  height: 26px;
+  color: #5f4a11;
+  border: 1px solid rgba(95, 74, 17, 0.4);
+  border-radius: 50%;
+  place-items: center;
+  transition: color 180ms ease, border-color 180ms ease, background 180ms ease, transform 180ms ease;
+}
+
+.track-list__play svg {
+  width: 14px;
+  height: 14px;
+  fill: currentColor;
+}
+
+.track-list__play:hover,
+.track-list__play:focus-visible,
+.track-list__play.is-selected {
+  color: #f5f0e6;
+  border-color: #17130b;
+  background: #17130b;
+  transform: scale(1.06);
+}
+
+.track-list__play.is-loading::after {
+  content: "";
+  position: absolute;
+  inset: -4px;
+  border: 1px solid #806517;
+  border-right-color: transparent;
+  border-radius: 50%;
+  animation: track-button-spin 700ms linear infinite;
+}
+
+.track-list__details {
+  display: grid;
+  grid-template-columns: minmax(0, 1fr) auto;
+  gap: 8px;
+  align-items: center;
+  width: 100%;
+  font: inherit;
+  line-height: inherit;
+  text-align: left;
+}
+
+.track-list__details span {
+  overflow: hidden;
+  text-overflow: ellipsis;
+  white-space: nowrap;
+}
+
+.track-list__details time {
+  transition: color 180ms ease;
+}
+
+.track-list__details:hover,
+.track-list__details:focus-visible,
+.track-list__details:hover time,
+.track-list__details:focus-visible time {
+  color: #9b7615;
+}
+
+@keyframes track-button-spin {
+  to { transform: rotate(360deg); }
+}
+
 .bandcamp-player {
   display: block;
   width: 100%;
@@ -1194,16 +1305,18 @@ main,
   border: 0;
 }
 
-.album-panel-button {
-  width: 100%;
-  margin-top: 18px;
+.bandcamp-selection-status {
+  min-height: 1.45em;
+  margin: 9px 0 0;
+  color: rgba(245, 240, 230, 0.6);
+  font-size: 0.68rem;
 }
 
-/* Immersive album player */
-.album-modal {
-  width: min(1160px, calc(100vw - 32px));
+/* Album and track details card */
+.album-details-modal {
+  width: min(920px, calc(100vw - 28px));
   max-width: none;
-  max-height: calc(100svh - 32px);
+  max-height: calc(100svh - 28px);
   padding: 0;
   overflow: visible;
   color: var(--paper);
@@ -1212,49 +1325,59 @@ main,
   font-family: var(--font-body);
 }
 
-.album-modal::backdrop {
+.album-details-modal::backdrop {
   background:
-    radial-gradient(circle at 22% 30%, rgba(225, 187, 45, 0.12), transparent 30%),
+    radial-gradient(circle at 22% 24%, rgba(225, 187, 45, 0.14), transparent 32%),
+    radial-gradient(circle at 82% 78%, rgba(166, 51, 45, 0.12), transparent 30%),
     rgba(2, 2, 3, 0.9);
-  -webkit-backdrop-filter: blur(14px) saturate(115%);
-  backdrop-filter: blur(14px) saturate(115%);
+  -webkit-backdrop-filter: blur(12px) saturate(112%);
+  backdrop-filter: blur(12px) saturate(112%);
 }
 
-.album-modal[open] {
-  animation: album-modal-in 320ms cubic-bezier(0.16, 0.82, 0.2, 1) both;
+.album-details-modal[open] {
+  animation: album-details-in 300ms cubic-bezier(0.16, 0.82, 0.2, 1) both;
 }
 
-.album-modal.is-fallback[open] {
+.album-details-modal[open]::backdrop {
+  animation: member-backdrop-in 260ms ease both;
+}
+
+.album-details-modal.is-fallback[open] {
   position: fixed;
-  inset: 16px;
+  inset: 14px;
   z-index: 1000;
   display: block;
   margin: auto;
 }
 
-.album-modal[open]::backdrop {
-  animation: member-backdrop-in 260ms ease both;
-}
-
-.album-modal__surface {
+.album-details-card {
   position: relative;
-  display: grid;
-  grid-template-columns: minmax(330px, 0.9fr) minmax(420px, 1.1fr);
-  max-height: calc(100svh - 32px);
+  max-height: calc(100svh - 28px);
   overflow-y: auto;
   overscroll-behavior: contain;
-  border: 1px solid rgba(225, 187, 45, 0.48);
+  border: 1px solid rgba(225, 187, 45, 0.5);
   background:
-    radial-gradient(circle at 92% 7%, rgba(166, 51, 45, 0.19), transparent 29%),
+    radial-gradient(circle at 96% 4%, rgba(166, 51, 45, 0.2), transparent 29%),
     #0d0d0f;
   box-shadow: 0 35px 130px rgba(0, 0, 0, 0.82);
 }
 
-.album-modal__close {
+.album-details-card::before {
+  content: "";
+  position: absolute;
+  top: 0;
+  left: clamp(28px, 6vw, 64px);
+  z-index: 2;
+  width: 96px;
+  height: 3px;
+  background: var(--gold);
+}
+
+.album-details-card__close {
   position: absolute;
   top: 16px;
   right: 16px;
-  z-index: 6;
+  z-index: 5;
   display: grid;
   width: 46px;
   height: 46px;
@@ -1270,163 +1393,144 @@ main,
   transition: color 180ms ease, border-color 180ms ease, transform 180ms ease;
 }
 
-.album-modal__close:hover {
+.album-details-card__close:hover {
   color: var(--gold-light);
   border-color: var(--gold);
   transform: rotate(5deg);
 }
 
-.album-modal__visual {
-  position: sticky;
-  top: 0;
-  display: grid;
-  min-height: min(760px, calc(100svh - 34px));
-  align-content: center;
-  justify-items: center;
-  padding: clamp(40px, 6vw, 74px);
+.album-details-card__header {
+  position: relative;
+  padding: clamp(44px, 7vw, 76px) clamp(28px, 6vw, 64px) clamp(32px, 5vw, 52px);
   overflow: hidden;
+  border-bottom: 1px solid var(--line);
   background:
-    linear-gradient(145deg, rgba(225, 187, 45, 0.13), transparent 42%),
-    #080809;
+    linear-gradient(120deg, rgba(225, 187, 45, 0.08), transparent 45%),
+    rgba(255, 255, 255, 0.012);
 }
 
-.album-modal__visual::before {
+.album-details-card__header::before {
   content: "";
   position: absolute;
   inset: 0;
-  opacity: 0.2;
-  background-image: repeating-linear-gradient(118deg, transparent 0 14px, rgba(225, 187, 45, 0.13) 14px 15px);
+  opacity: 0.18;
+  background-image: repeating-linear-gradient(118deg, transparent 0 18px, rgba(225, 187, 45, 0.18) 18px 19px);
   pointer-events: none;
 }
 
-.album-modal__orbit {
-  position: absolute;
-  width: min(38vw, 470px);
-  aspect-ratio: 1;
-  border: 1px solid rgba(225, 187, 45, 0.25);
-  border-radius: 50%;
-  box-shadow:
-    0 0 0 26px rgba(225, 187, 45, 0.025),
-    0 0 0 58px rgba(225, 187, 45, 0.018);
-  animation: album-orbit-turn 24s linear infinite;
+.album-details-card__header > * {
+  position: relative;
+  z-index: 1;
 }
 
-.album-modal__orbit::before,
-.album-modal__orbit::after {
-  content: "";
-  position: absolute;
-  border-radius: 50%;
-  background: var(--gold);
-  box-shadow: 0 0 18px rgba(225, 187, 45, 0.62);
+.album-details-card__eyebrow {
+  margin-bottom: 13px;
+  color: var(--gold-light);
+  font-family: var(--font-display);
+  font-size: 0.62rem;
+  font-weight: 600;
+  letter-spacing: 0.15em;
+  text-transform: uppercase;
 }
 
-.album-modal__orbit::before {
-  top: 9%;
-  left: 15%;
-  width: 8px;
-  height: 8px;
+.album-details-card__header h2 {
+  max-width: calc(100% - 60px);
+  margin-bottom: 14px;
+  font-family: var(--font-display);
+  font-size: clamp(2rem, 7vw, 4.6rem);
+  letter-spacing: -0.045em;
+  line-height: 1.04;
 }
 
-.album-modal__orbit::after {
-  right: 4%;
-  bottom: 27%;
-  width: 5px;
-  height: 5px;
+.album-details-card__meta {
+  margin: 0;
+  color: var(--muted);
+  font-size: 0.82rem;
+  letter-spacing: 0.06em;
+  text-transform: uppercase;
 }
 
-.album-modal__artwork {
-  position: relative;
-  z-index: 2;
-  width: min(100%, 400px);
-  aspect-ratio: 1;
-  overflow: hidden;
-  box-shadow:
-    18px 20px 0 rgba(225, 187, 45, 0.13),
-    0 34px 90px rgba(0, 0, 0, 0.62);
-  transform: rotate(-1.3deg);
+.album-details-card__sections {
+  display: grid;
 }
 
-.album-modal__artwork img {
-  width: 100%;
-  height: 100%;
-  object-fit: cover;
+.album-details-card__section {
+  padding: clamp(28px, 5vw, 52px) clamp(28px, 6vw, 64px);
 }
 
-.album-modal__artwork span {
-  position: absolute;
-  inset: 0;
-  background: linear-gradient(112deg, transparent 26%, rgba(255, 244, 180, 0.3) 45%, transparent 62%);
-  mix-blend-mode: screen;
-  animation: album-modal-shine 7s ease-in-out infinite;
+.album-details-card__section + .album-details-card__section {
+  border-top: 1px solid var(--line);
 }
 
-.album-modal__visual > p {
-  position: relative;
-  z-index: 2;
-  margin: 32px 0 0;
-  color: rgba(245, 240, 230, 0.62);
+.album-details-card__section h3 {
+  margin-bottom: 22px;
+  color: var(--paper);
   font-family: var(--font-display);
-  font-size: 0.55rem;
-  letter-spacing: 0.12em;
-  text-transform: uppercase;
+  font-size: clamp(1rem, 2.4vw, 1.45rem);
+  line-height: 1.3;
 }
 
-.album-modal__content {
-  padding: clamp(44px, 6vw, 76px);
+.album-details-card__fields {
+  display: grid;
+  grid-template-columns: repeat(2, minmax(0, 1fr));
+  margin: 0;
+  border-top: 1px solid var(--line);
+  border-left: 1px solid var(--line);
 }
 
-.album-modal__content h2 {
-  margin-bottom: 18px;
+.album-details-card__field {
+  min-width: 0;
+  min-height: 76px;
+  padding: 13px 15px;
+  border-right: 1px solid var(--line);
+  border-bottom: 1px solid var(--line);
+  background: rgba(255, 255, 255, 0.018);
+}
+
+.album-details-card__field dt {
+  margin-bottom: 7px;
+  color: var(--gold-light);
   font-family: var(--font-display);
-  font-size: clamp(2.1rem, 5vw, 4.5rem);
-  line-height: 1;
+  font-size: 0.52rem;
+  font-weight: 600;
+  letter-spacing: 0.08em;
+  line-height: 1.45;
+  text-transform: uppercase;
 }
 
-.album-modal__content > p:not(.release-kicker) {
-  max-width: 55ch;
-  margin-bottom: 0;
-  color: var(--muted);
+.album-details-card__field dd {
+  margin: 0;
+  color: var(--paper);
+  font-size: 0.9rem;
+  line-height: 1.55;
+  overflow-wrap: anywhere;
+  white-space: pre-line;
 }
 
-.album-modal__player {
-  display: block;
-  width: 100%;
-  height: 300px;
-  margin-top: 30px;
-  border: 0;
+.album-details-card__field dd.is-empty {
+  color: rgba(245, 240, 230, 0.3);
 }
 
-.album-modal__links {
-  display: grid;
-  grid-template-columns: repeat(2, minmax(0, 1fr));
-  margin-top: 24px;
-  border-top: 1px solid var(--line);
-  border-left: 1px solid var(--line);
+.album-details-card__section--wide .album-details-card__fields {
+  grid-template-columns: 1fr;
 }
 
-.album-modal__links a {
-  display: flex;
-  min-height: 52px;
-  align-items: center;
-  justify-content: space-between;
-  padding: 12px 14px;
-  border-right: 1px solid var(--line);
-  border-bottom: 1px solid var(--line);
-  font-family: var(--font-display);
-  font-size: 0.56rem;
-  letter-spacing: 0.04em;
-  transition: color 180ms ease, background 180ms ease;
+.album-details-card__section--wide .album-details-card__field {
+  display: grid;
+  grid-template-columns: minmax(140px, 0.32fr) minmax(0, 1fr);
+  gap: 22px;
+  align-items: start;
+  min-height: 68px;
 }
 
-.album-modal__links a:hover {
-  color: var(--ink);
-  background: var(--gold);
+.album-details-card__section--wide .album-details-card__field dt {
+  margin: 2px 0 0;
 }
 
-@keyframes album-modal-in {
+@keyframes album-details-in {
   from {
     opacity: 0;
-    transform: translateY(22px) scale(0.975);
+    transform: translateY(20px) scale(0.98);
   }
 
   to {
@@ -1435,15 +1539,6 @@ main,
   }
 }
 
-@keyframes album-orbit-turn {
-  to { transform: rotate(360deg); }
-}
-
-@keyframes album-modal-shine {
-  0%, 52% { transform: translateX(-125%); }
-  78%, 100% { transform: translateX(125%); }
-}
-
 .platforms {
   position: relative;
   z-index: 1;
@@ -2286,12 +2381,22 @@ main,
   }
 
   .track-list--insert li {
-    grid-template-columns: 19px minmax(0, 1fr) auto;
+    grid-template-columns: 19px 24px minmax(0, 1fr);
     gap: 4px;
     padding: clamp(3px, 0.8vw, 6px) 0;
     font-size: clamp(0.55rem, 1.7vw, 0.72rem);
   }
 
+  .track-list__play {
+    width: 22px;
+    height: 22px;
+  }
+
+  .track-list__play svg {
+    width: 12px;
+    height: 12px;
+  }
+
   .album-insert-toggle {
     right: 0;
     bottom: -30px;
@@ -2301,30 +2406,8 @@ main,
     display: none;
   }
 
-  .album-modal__surface {
-    grid-template-columns: 1fr;
-  }
-
-  .album-modal__visual {
-    position: relative;
-    min-height: auto;
-    padding: 54px 58px 48px;
-  }
-
-  .album-modal__orbit {
-    width: min(62vw, 360px);
-  }
-
-  .album-modal__artwork {
-    width: min(62vw, 300px);
-  }
-
-  .album-modal__visual > p {
-    margin-top: 24px;
-  }
-
-  .album-modal__content {
-    padding: 42px clamp(24px, 7vw, 56px) 54px;
+  .album-details-card__section--wide .album-details-card__field {
+    grid-template-columns: minmax(120px, 0.35fr) minmax(0, 1fr);
   }
   .video-feature { gap: 18px; }
   .story-copy { padding-top: 0; }
@@ -2355,48 +2438,50 @@ main,
     font-size: 0.78rem;
   }
 
+  .album-insert__title-action {
+    display: none;
+  }
+
   .album-insert__catalog {
     display: none;
   }
 
-  .album-modal {
-    width: calc(100vw - 16px);
-    max-height: calc(100svh - 16px);
+  .album-details-modal {
+    width: calc(100vw - 12px);
+    max-height: calc(100svh - 12px);
   }
 
-  .album-modal__surface {
-    max-height: calc(100svh - 16px);
+  .album-details-card {
+    max-height: calc(100svh - 12px);
   }
 
-  .album-modal__close {
+  .album-details-card__close {
     top: 10px;
     right: 10px;
     width: 42px;
     height: 42px;
   }
 
-  .album-modal__visual {
-    padding: 44px 34px 36px;
-  }
-
-  .album-modal__artwork {
-    width: min(66vw, 250px);
+  .album-details-card__header {
+    padding: 48px 20px 30px;
   }
 
-  .album-modal__content {
-    padding: 34px 22px 42px;
+  .album-details-card__header h2 {
+    max-width: calc(100% - 44px);
+    font-size: clamp(1.65rem, 10vw, 2.8rem);
   }
 
-  .album-modal__content h2 {
-    font-size: clamp(2rem, 13vw, 3.3rem);
+  .album-details-card__section {
+    padding: 27px 18px;
   }
 
-  .album-modal__player {
-    height: 310px;
+  .album-details-card__fields {
+    grid-template-columns: 1fr;
   }
 
-  .album-modal__links {
-    grid-template-columns: 1fr;
+  .album-details-card__section--wide .album-details-card__field {
+    display: block;
+    min-height: 76px;
   }
 
   .platforms { grid-template-columns: 1fr; }
@@ -2439,9 +2524,7 @@ main,
     transform: none;
   }
 
-  .album-sleeve__foil::after,
-  .album-modal__orbit,
-  .album-modal__artwork span {
+  .album-sleeve__foil::after {
     animation: none !important;
   }
 
diff --git a/index.html b/index.html
index 9b814d2..1fb4bff 100644
--- a/index.html
+++ b/index.html
@@ -107,12 +107,49 @@
             <div class="album-object" data-album-sleeve>
               <div class="album-insert" id="album-track-insert">
                 <p class="album-insert__kicker">Inside the sleeve</p>
-                <p class="album-insert__title">EP No. 1</p>
+                <button
+                  class="album-insert__title"
+                  type="button"
+                  data-album-detail="album"
+                  aria-haspopup="dialog"
+                  aria-controls="album-details-modal"
+                >
+                  <span>EP No. 1</span>
+                  <span class="album-insert__title-action">Album details ↗</span>
+                </button>
                 <ol class="track-list track-list--insert">
-                  <li><span>Zen &amp; Hummus</span><time>08:11</time></li>
-                  <li><span>Slap Happy</span><time>05:18</time></li>
-                  <li><span>Nomuzo</span><time>05:48</time></li>
-                  <li><span>Five</span><time>05:00</time></li>
+                  <li>
+                    <button class="track-list__play" type="button" data-bandcamp-track="2207760647" data-track-title="Zen &amp; Hummus" aria-label="Play Zen &amp; Hummus in the Bandcamp player">
+                      <svg viewBox="0 0 24 24" aria-hidden="true" focusable="false"><path d="m9 7 8 5-8 5Z"></path></svg>
+                    </button>
+                    <button class="track-list__details" type="button" data-album-detail="zen-hummus" aria-haspopup="dialog" aria-controls="album-details-modal">
+                      <span>Zen &amp; Hummus</span><time datetime="PT8M11S">08:11</time>
+                    </button>
+                  </li>
+                  <li>
+                    <button class="track-list__play" type="button" data-bandcamp-track="1478400786" data-track-title="Slap Happy" aria-label="Play Slap Happy in the Bandcamp player">
+                      <svg viewBox="0 0 24 24" aria-hidden="true" focusable="false"><path d="m9 7 8 5-8 5Z"></path></svg>
+                    </button>
+                    <button class="track-list__details" type="button" data-album-detail="slap-happy" aria-haspopup="dialog" aria-controls="album-details-modal">
+                      <span>Slap Happy</span><time datetime="PT5M18S">05:18</time>
+                    </button>
+                  </li>
+                  <li>
+                    <button class="track-list__play" type="button" data-bandcamp-track="3384979077" data-track-title="Nomuzo" aria-label="Play Nomuzo in the Bandcamp player">
+                      <svg viewBox="0 0 24 24" aria-hidden="true" focusable="false"><path d="m9 7 8 5-8 5Z"></path></svg>
+                    </button>
+                    <button class="track-list__details" type="button" data-album-detail="nomuzo" aria-haspopup="dialog" aria-controls="album-details-modal">
+                      <span>Nomuzo</span><time datetime="PT5M48S">05:48</time>
+                    </button>
+                  </li>
+                  <li>
+                    <button class="track-list__play" type="button" data-bandcamp-track="767259295" data-track-title="Five" aria-label="Play Five in the Bandcamp player">
+                      <svg viewBox="0 0 24 24" aria-hidden="true" focusable="false"><path d="m9 7 8 5-8 5Z"></path></svg>
+                    </button>
+                    <button class="track-list__details" type="button" data-album-detail="five" aria-haspopup="dialog" aria-controls="album-details-modal">
+                      <span>Five</span><time datetime="PT5M">05:00</time>
+                    </button>
+                  </li>
                 </ol>
                 <p class="album-insert__catalog">BAL · 001 / Tokyo</p>
               </div>
@@ -120,17 +157,17 @@
               <button
                 class="release-art album-sleeve"
                 type="button"
-                data-album-open
+                data-album-detail="album"
                 aria-haspopup="dialog"
-                aria-controls="album-modal"
-                aria-label="Open the immersive EP No. 1 listening panel"
+                aria-controls="album-details-modal"
+                aria-label="Open credits and details for EP No. 1"
               >
                 <span class="album-sleeve__cover">
                   <img src="img/ep-no-1.webp" width="900" height="900" alt="Yellow cover artwork for Balaganist EP No. 1">
                   <span class="album-sleeve__foil" aria-hidden="true"></span>
                   <span class="album-sleeve__edge" aria-hidden="true"></span>
                 </span>
-                <span class="album-sleeve__action">Enter album <b aria-hidden="true">↗</b></span>
+                <span class="album-sleeve__action">Album details <b aria-hidden="true">↗</b></span>
               </button>
 
               <button
@@ -152,10 +189,16 @@
               <p class="release-kicker">Featured release · 2016</p>
               <h3>EP No. 1</h3>
               <p>Four original instrumental tracks shaped by progressive-rock structures, jazz instincts and sounds gathered from around the world.</p>
-              <iframe class="bandcamp-player" title="Listen to EP No. 1 by Balaganist on Bandcamp" src="https://bandcamp.com/EmbeddedPlayer/album=506663424/size=large/bgcol=111111/linkcol=e1bb2d/tracklist=false/artwork=small/transparent=true/" seamless></iframe>
-              <button class="button button--ghost album-panel-button" type="button" data-album-open aria-haspopup="dialog" aria-controls="album-modal">
-                Open immersive player <span aria-hidden="true">↗</span>
-              </button>
+              <iframe
+                class="bandcamp-player"
+                id="bandcamp-player"
+                title="Listen to EP No. 1 by Balaganist on Bandcamp"
+                src="https://bandcamp.com/EmbeddedPlayer/album=506663424/size=large/bgcol=111111/linkcol=e1bb2d/tracklist=false/artwork=small/transparent=true/"
+                data-bandcamp-base="https://bandcamp.com/EmbeddedPlayer/album=506663424/size=large/bgcol=111111/linkcol=e1bb2d/tracklist=false/artwork=small/"
+                allow="autoplay"
+                seamless
+              ></iframe>
+              <p class="bandcamp-selection-status" id="bandcamp-selection-status" role="status" aria-live="polite"></p>
             </div>
           </div>
 
@@ -427,44 +470,21 @@
         </main>
 
     <dialog
-      class="album-modal"
-      id="album-modal"
-      aria-labelledby="album-modal-title"
-      aria-describedby="album-modal-description"
+      class="album-details-modal"
+      id="album-details-modal"
+      aria-labelledby="album-details-title"
+      aria-describedby="album-details-meta"
     >
-      <div class="album-modal__surface">
-        <button class="album-modal__close" type="button" data-album-modal-close aria-label="Close album player">×</button>
-
-        <div class="album-modal__visual">
-          <div class="album-modal__orbit" aria-hidden="true"></div>
-          <div class="album-modal__artwork">
-            <img src="img/ep-no-1.webp" width="900" height="900" alt="Yellow cover artwork for Balaganist EP No. 1">
-            <span aria-hidden="true"></span>
-          </div>
-          <p>Balaganist · Tokyo · 2016</p>
-        </div>
+      <div class="album-details-card">
+        <button class="album-details-card__close" type="button" data-album-details-close aria-label="Close album details">×</button>
 
-        <div class="album-modal__content">
-          <p class="release-kicker">Featured release</p>
-          <h2 id="album-modal-title">EP No. 1</h2>
-          <p id="album-modal-description">Four original instrumental tracks shaped by progressive-rock structures, jazz instincts and sounds gathered from around the world.</p>
-
-          <iframe
-  class="album-modal__player"
-  title="Play EP No. 1 by Balaganist on Bandcamp"
-  data-src="https://bandcamp.com/EmbeddedPlayer/album=506663424/size=large/bgcol=0d0d0f/linkcol=e1bb2d/tracklist=true/artwork=none/transparent=true/"
-  src="about:blank"
-  allow="autoplay"
-  seamless
-></iframe>
-
-          <nav class="album-modal__links" aria-label="Listen on another service">
-            <a href="https://balaganist.bandcamp.com/album/ep-no-1" target="_blank" rel="noopener noreferrer">Bandcamp ↗</a>
-            <a href="https://open.spotify.com/artist/6Go8a0WKJ38QSuHkHClr1R" target="_blank" rel="noopener noreferrer">Spotify ↗</a>
-            <a href="https://music.apple.com/us/artist/balaganist/1107618913" target="_blank" rel="noopener noreferrer">Apple Music ↗</a>
-            <a href="https://www.youtube.com/user/balaganistjapan" target="_blank" rel="noopener noreferrer">YouTube ↗</a>
-          </nav>
-        </div>
+        <header class="album-details-card__header">
+          <p class="album-details-card__eyebrow" id="album-details-eyebrow"></p>
+          <h2 id="album-details-title"></h2>
+          <p class="album-details-card__meta" id="album-details-meta"></p>
+        </header>
+
+        <div class="album-details-card__sections" id="album-details-sections"></div>
       </div>
     </dialog>
 
diff --git a/js/mainFunctions.js b/js/mainFunctions.js
index 1e0de79..df0d479 100644
--- a/js/mainFunctions.js
+++ b/js/mainFunctions.js
@@ -3,6 +3,198 @@
 
   const reduceMotion = window.matchMedia("(prefers-reduced-motion: reduce)").matches;
 
+  // Add credits and stories by replacing the blank strings in this object.
+  const albumDetailProfiles = {
+    album: {
+      eyebrow: "Album details",
+      title: "EP No. 1",
+      meta: "Balaganist · 2016 · Four tracks",
+      sections: [
+        {
+          title: "Album credits",
+          fields: {
+            "Produced by": "",
+            "Executive producer": "",
+            "Recorded by": "",
+            "Recorded at": "",
+            "Mixed by": "",
+            "Mastered by": "",
+            "Artwork / design": "",
+            "Photography": "",
+            "Label / catalog": "",
+            "Release date": ""
+          }
+        },
+        {
+          title: "Inside the album",
+          layout: "wide",
+          fields: {
+            "Background / story": "",
+            "Recording notes": "",
+            "Fun facts": "",
+            "Special thanks": ""
+          }
+        }
+      ]
+    },
+
+    "zen-hummus": {
+      eyebrow: "Track 01",
+      title: "Zen & Hummus",
+      meta: "08:11 · EP No. 1",
+      sections: [
+        {
+          title: "Track credits",
+          fields: {
+            "Composed by": "",
+            "Arranged by": "",
+            "Guitar": "",
+            "Bass": "",
+            "Drums": "",
+            "Violin": "",
+            "Saxophone": "",
+            "Keyboards": "",
+            "Percussion": "",
+            "Additional musicians": "",
+            "Produced by": "",
+            "Recorded by": "",
+            "Recorded at": "",
+            "Mixed by": "",
+            "Mastered by": ""
+          }
+        },
+        {
+          title: "Behind the track",
+          layout: "wide",
+          fields: {
+            "Background / story": "",
+            "Recording notes": "",
+            "Fun facts": "",
+            "Time signature(s)": "",
+            "Gear / sounds": ""
+          }
+        }
+      ]
+    },
+
+    "slap-happy": {
+      eyebrow: "Track 02",
+      title: "Slap Happy",
+      meta: "05:18 · EP No. 1",
+      sections: [
+        {
+          title: "Track credits",
+          fields: {
+            "Composed by": "",
+            "Arranged by": "",
+            "Guitar": "",
+            "Bass": "",
+            "Drums": "",
+            "Violin": "",
+            "Saxophone": "",
+            "Keyboards": "",
+            "Percussion": "",
+            "Additional musicians": "",
+            "Produced by": "",
+            "Recorded by": "",
+            "Recorded at": "",
+            "Mixed by": "",
+            "Mastered by": ""
+          }
+        },
+        {
+          title: "Behind the track",
+          layout: "wide",
+          fields: {
+            "Background / story": "",
+            "Recording notes": "",
+            "Fun facts": "",
+            "Time signature(s)": "",
+            "Gear / sounds": ""
+          }
+        }
+      ]
+    },
+
+    nomuzo: {
+      eyebrow: "Track 03",
+      title: "Nomuzo",
+      meta: "05:48 · EP No. 1",
+      sections: [
+        {
+          title: "Track credits",
+          fields: {
+            "Composed by": "",
+            "Arranged by": "",
+            "Guitar": "",
+            "Bass": "",
+            "Drums": "",
+            "Violin": "",
+            "Saxophone": "",
+            "Keyboards": "",
+            "Percussion": "",
+            "Additional musicians": "",
+            "Produced by": "",
+            "Recorded by": "",
+            "Recorded at": "",
+            "Mixed by": "",
+            "Mastered by": ""
+          }
+        },
+        {
+          title: "Behind the track",
+          layout: "wide",
+          fields: {
+            "Background / story": "",
+            "Recording notes": "",
+            "Fun facts": "",
+            "Time signature(s)": "",
+            "Gear / sounds": ""
+          }
+        }
+      ]
+    },
+
+    five: {
+      eyebrow: "Track 04",
+      title: "Five",
+      meta: "05:00 · EP No. 1",
+      sections: [
+        {
+          title: "Track credits",
+          fields: {
+            "Composed by": "",
+            "Arranged by": "",
+            "Guitar": "",
+            "Bass": "",
+            "Drums": "",
+            "Violin": "",
+            "Saxophone": "",
+            "Keyboards": "",
+            "Percussion": "",
+            "Additional musicians": "",
+            "Produced by": "",
+            "Recorded by": "",
+            "Recorded at": "",
+            "Mixed by": "",
+            "Mastered by": ""
+          }
+        },
+        {
+          title: "Behind the track",
+          layout: "wide",
+          fields: {
+            "Background / story": "",
+            "Recording notes": "",
+            "Fun facts": "",
+            "Time signature(s)": "",
+            "Gear / sounds": ""
+          }
+        }
+      ]
+    }
+  };
+
   const memberProfiles = {
     ron: {
       name: "Ron J. Ward",
@@ -329,61 +521,56 @@
   }
 
   function setupAlbumExperience() {
-  const albumObject = document.querySelector("[data-album-sleeve]");
-  const albumCover = albumObject?.querySelector(".album-sleeve");
-  const albumInsert = albumObject?.querySelector(".album-insert");
-  const insertToggle = albumObject?.querySelector("[data-album-insert-toggle]");
-  const modal = document.getElementById("album-modal");
-  const modalSurface = modal?.querySelector(".album-modal__surface");
-  const closeButton = modal?.querySelector("[data-album-modal-close]");
-  const player = modal?.querySelector(".album-modal__player");
-  const openButtons = document.querySelectorAll("[data-album-open]");
-
-  if (
-    !albumObject ||
-    !albumCover ||
-    !albumInsert ||
-    !insertToggle ||
-    !modal ||
-    !modalSurface ||
-    !closeButton ||
-    !player ||
-    !openButtons.length
-  ) {
-    return;
-  }
+    const albumObject = document.querySelector("[data-album-sleeve]");
+    const albumCover = albumObject?.querySelector(".album-sleeve");
+    const albumInsert = albumObject?.querySelector(".album-insert");
+    const insertToggle = albumObject?.querySelector("[data-album-insert-toggle]");
+    const trackButtons = albumObject?.querySelectorAll("[data-bandcamp-track]");
+    const player = document.getElementById("bandcamp-player");
+    const playerStatus = document.getElementById("bandcamp-selection-status");
 
-  let lastTrigger = null;
-  let tiltFrame = 0;
-  let insertCloseTimer = null;
-
-  function finishInsertClose() {
-    albumObject.classList.remove("is-insert-closing");
-    insertCloseTimer = null;
-  }
+    if (
+      !albumObject ||
+      !albumCover ||
+      !albumInsert ||
+      !insertToggle ||
+      !trackButtons?.length ||
+      !player
+    ) {
+      return;
+    }
 
-  function setInsertOpen(open) {
-    const wasOpen = albumObject.classList.contains("is-insert-open");
+    let tiltFrame = 0;
+    let insertCloseTimer = null;
+    let pendingTrack = null;
 
-    if (insertCloseTimer !== null) {
-      window.clearTimeout(insertCloseTimer);
+    function finishInsertClose() {
+      albumObject.classList.remove("is-insert-closing");
       insertCloseTimer = null;
     }
 
-    albumObject.classList.remove("is-insert-closing");
+    function setInsertOpen(open) {
+      const wasOpen = albumObject.classList.contains("is-insert-open");
 
-    if (open) {
-      albumObject.classList.add("is-insert-open");
-    } else if (wasOpen) {
-      albumObject.classList.remove("is-insert-open");
-      albumObject.classList.add("is-insert-closing");
-      insertCloseTimer = window.setTimeout(finishInsertClose, 680);
-    } else {
-      albumObject.classList.remove("is-insert-open");
-    }
+      if (insertCloseTimer !== null) {
+        window.clearTimeout(insertCloseTimer);
+        insertCloseTimer = null;
+      }
 
-    insertToggle.setAttribute("aria-expanded", String(open));
-  }
+      albumObject.classList.remove("is-insert-closing");
+
+      if (open) {
+        albumObject.classList.add("is-insert-open");
+      } else if (wasOpen) {
+        albumObject.classList.remove("is-insert-open");
+        albumObject.classList.add("is-insert-closing");
+        insertCloseTimer = window.setTimeout(finishInsertClose, 680);
+      } else {
+        albumObject.classList.remove("is-insert-open");
+      }
+
+      insertToggle.setAttribute("aria-expanded", String(open));
+    }
 
     function resetTilt() {
       if (tiltFrame) {
@@ -397,12 +584,144 @@
       albumCover.style.setProperty("--foil-y", "24%");
     }
 
-    function finishClose() {
-      document.body.classList.remove("album-open");
+    function playBandcampTrack(button) {
+      const trackId = button.dataset.bandcampTrack;
+      const trackTitle = button.dataset.trackTitle || "Selected track";
+      const baseUrl = player.dataset.bandcampBase;
+
+      if (!trackId || !baseUrl) return;
 
-      if (player.getAttribute("src") !== "about:blank") {
-        player.setAttribute("src", "about:blank");
+      trackButtons.forEach(function (trackButton) {
+        trackButton.classList.remove("is-loading", "is-selected");
+      });
+
+      button.classList.add("is-loading", "is-selected");
+      pendingTrack = { button: button, title: trackTitle };
+      player.title = `Play ${trackTitle} by Balaganist on Bandcamp`;
+      player.setAttribute(
+        "src",
+        `${baseUrl}track=${encodeURIComponent(trackId)}/autoplay=true/transparent=true/`
+      );
+
+      if (playerStatus) {
+        playerStatus.textContent = `Starting “${trackTitle}” in Bandcamp…`;
       }
+    }
+
+    insertToggle.addEventListener("click", function () {
+      setInsertOpen(insertToggle.getAttribute("aria-expanded") !== "true");
+    });
+
+    trackButtons.forEach(function (button) {
+      button.addEventListener("click", function () {
+        playBandcampTrack(button);
+      });
+    });
+
+    player.addEventListener("load", function () {
+      if (!pendingTrack) return;
+
+      pendingTrack.button.classList.remove("is-loading");
+
+      if (playerStatus) {
+        playerStatus.textContent = `“${pendingTrack.title}” is loaded in the Bandcamp player.`;
+      }
+
+      pendingTrack = null;
+    });
+
+    if (!reduceMotion && window.matchMedia("(pointer: fine)").matches) {
+      albumCover.addEventListener("pointermove", function (event) {
+        const bounds = albumCover.getBoundingClientRect();
+        const x = Math.max(0, Math.min(1, (event.clientX - bounds.left) / bounds.width));
+        const y = Math.max(0, Math.min(1, (event.clientY - bounds.top) / bounds.height));
+
+        if (tiltFrame) window.cancelAnimationFrame(tiltFrame);
+
+        tiltFrame = window.requestAnimationFrame(function () {
+          albumCover.style.setProperty("--sleeve-rotate-x", `${((0.5 - y) * 6).toFixed(2)}deg`);
+          albumCover.style.setProperty("--sleeve-rotate-y", `${((x - 0.5) * 8).toFixed(2)}deg`);
+          albumCover.style.setProperty("--foil-x", `${(x * 100).toFixed(1)}%`);
+          albumCover.style.setProperty("--foil-y", `${(y * 100).toFixed(1)}%`);
+          tiltFrame = 0;
+        });
+      });
+
+      albumCover.addEventListener("pointerleave", resetTilt);
+      albumCover.addEventListener("blur", resetTilt);
+    }
+  }
+
+  function setupAlbumDetails() {
+    const modal = document.getElementById("album-details-modal");
+    const modalCard = modal?.querySelector(".album-details-card");
+    const closeButton = modal?.querySelector("[data-album-details-close]");
+    const eyebrow = document.getElementById("album-details-eyebrow");
+    const title = document.getElementById("album-details-title");
+    const meta = document.getElementById("album-details-meta");
+    const sectionsContainer = document.getElementById("album-details-sections");
+    const detailButtons = document.querySelectorAll("[data-album-detail]");
+
+    if (
+      !modal ||
+      !modalCard ||
+      !closeButton ||
+      !eyebrow ||
+      !title ||
+      !meta ||
+      !sectionsContainer ||
+      !detailButtons.length
+    ) {
+      return;
+    }
+
+    let lastTrigger = null;
+
+    function renderSections(sections) {
+      const fragment = document.createDocumentFragment();
+
+      sections.forEach(function (sectionData) {
+        const section = document.createElement("section");
+        const heading = document.createElement("h3");
+        const fields = document.createElement("dl");
+
+        section.className = "album-details-card__section";
+        if (sectionData.layout === "wide") {
+          section.classList.add("album-details-card__section--wide");
+        }
+
+        heading.textContent = sectionData.title;
+        fields.className = "album-details-card__fields";
+
+        Object.entries(sectionData.fields).forEach(function ([label, value]) {
+          const row = document.createElement("div");
+          const term = document.createElement("dt");
+          const description = document.createElement("dd");
+          const text = String(value || "").trim();
+
+          row.className = "album-details-card__field";
+          term.textContent = label;
+          description.textContent = text || "—";
+
+          if (!text) {
+            description.classList.add("is-empty");
+            description.setAttribute("aria-label", "Not added yet");
+          }
+
+          row.append(term, description);
+          fields.appendChild(row);
+        });
+
+        section.append(heading, fields);
+        fragment.appendChild(section);
+      });
+
+      sectionsContainer.replaceChildren(fragment);
+    }
+
+    function finishClose() {
+      document.body.classList.remove("album-details-open");
+      modal.classList.remove("is-fallback");
 
       if (lastTrigger) {
         lastTrigger.focus({ preventScroll: true });
@@ -417,19 +736,22 @@
         modal.close();
       } else {
         modal.removeAttribute("open");
-        modal.classList.remove("is-fallback");
         finishClose();
       }
     }
 
-    function openModal(trigger) {
-      lastTrigger = trigger;
-      modalSurface.scrollTop = 0;
-      document.body.classList.add("album-open");
+    function openDetails(trigger) {
+      const profile = albumDetailProfiles[trigger.dataset.albumDetail];
+      if (!profile) return;
 
-      if (player.getAttribute("src") === "about:blank" && player.dataset.src) {
-        player.setAttribute("src", player.dataset.src);
-      }
+      eyebrow.textContent = profile.eyebrow;
+      title.textContent = profile.title;
+      meta.textContent = profile.meta;
+      renderSections(profile.sections);
+
+      lastTrigger = trigger;
+      modalCard.scrollTop = 0;
+      document.body.classList.add("album-details-open");
 
       if (!modal.hasAttribute("open")) {
         if (typeof modal.showModal === "function") {
@@ -437,17 +759,14 @@
         } else {
           modal.classList.add("is-fallback");
           modal.setAttribute("open", "");
+          closeButton.focus();
         }
       }
     }
 
-    insertToggle.addEventListener("click", function () {
-      setInsertOpen(insertToggle.getAttribute("aria-expanded") !== "true");
-    });
-
-    openButtons.forEach(function (button) {
+    detailButtons.forEach(function (button) {
       button.addEventListener("click", function () {
-        openModal(button);
+        openDetails(button);
       });
     });
 
@@ -464,26 +783,11 @@
 
     modal.addEventListener("close", finishClose);
 
-    if (!reduceMotion && window.matchMedia("(pointer: fine)").matches) {
-      albumCover.addEventListener("pointermove", function (event) {
-        const bounds = albumCover.getBoundingClientRect();
-        const x = Math.max(0, Math.min(1, (event.clientX - bounds.left) / bounds.width));
-        const y = Math.max(0, Math.min(1, (event.clientY - bounds.top) / bounds.height));
-
-        if (tiltFrame) window.cancelAnimationFrame(tiltFrame);
-
-        tiltFrame = window.requestAnimationFrame(function () {
-          albumCover.style.setProperty("--sleeve-rotate-x", `${((0.5 - y) * 6).toFixed(2)}deg`);
-          albumCover.style.setProperty("--sleeve-rotate-y", `${((x - 0.5) * 8).toFixed(2)}deg`);
-          albumCover.style.setProperty("--foil-x", `${(x * 100).toFixed(1)}%`);
-          albumCover.style.setProperty("--foil-y", `${(y * 100).toFixed(1)}%`);
-          tiltFrame = 0;
-        });
-      });
-
-      albumCover.addEventListener("pointerleave", resetTilt);
-      albumCover.addEventListener("blur", resetTilt);
-    }
+    window.addEventListener("keydown", function (event) {
+      if (event.key === "Escape" && modal.classList.contains("is-fallback")) {
+        closeModal();
+      }
+    });
   }
 
   function setupMemberBios() {
@@ -870,6 +1174,7 @@
     setupRevealAnimations();
     setupMemberTilt();
     setupAlbumExperience();
+    setupAlbumDetails();
     setupMemberBios();
     renderShows();
     setupContactForm();
-- 
2.51.1

